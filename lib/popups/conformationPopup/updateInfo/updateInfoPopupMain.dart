import 'package:flutter/material.dart';

class UpdateInfoPopupMain extends StatelessWidget {
  final int variation;
const UpdateInfoPopupMain({ super.key, required this.variation });

  @override
  Widget build(BuildContext context){
    return Container();
  }
}